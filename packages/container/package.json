{
  "name": "@rea-scripts/container",
  "version": "0.0.3",
  "description": "rea脚手架",
  "main": "dist/index.js",
  "scripts": {
    "dev": "rimraf ./dist && tsc --watch",
    "build": "rimraf ./dist && tsc -d",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0",
    "lint:script": "eslint --ext .ts,.js --fix ./src",
    "prepublishOnly": "yarn build"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "ts",
    "node"
  ],
  "author": "xzd",
  "license": "MIT",
  "dependencies": {
    "@rea-scripts/registry-component": "^0.0.3",
    "@rea-scripts/registry-default": "^0.0.3",
    "@rea-scripts/registry-node": "^0.0.3",
    "@rea-scripts/registry-project": "^0.0.3",
    "@rea-scripts/util": "^0.0.3"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^2.1.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  },
  "maintainers": [
    "xzd"
  ],
  "publishConfig": {
    "access": "public"
  },
  "gitHead": "7a4a4976076c2c4c68b6bf82cb7ea9bb13a745d1"
}
